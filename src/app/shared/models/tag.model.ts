export interface Tag {
  tagId: string, // id of the tag, same as tag itself
  uid: string, // id of the user who created this tag
  tag: string, // tag
  lastUpdateDate: object, 
  creationDate: object 
}